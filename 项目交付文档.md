# On-Call 助手项目交付文档

## 1. 项目概述

On-Call 助手是一个面向值班工程师的 SOP 检索与智能问答系统。项目读取 `code/data` 下的 SOP HTML 文件，提供关键词搜索、语义搜索和 Agent 对话三阶段能力。

本项目对应 `coding-exam/question-1` 的考核要求，重点展示：

- HTTP API 能力。
- 前端可演示页面。
- SOP HTML 解析与检索。
- 语义搜索 fallback。
- 受控工具调用的 Agent。
- 自动化验证脚本。

## 2. 交付内容

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 项目首页 README | `README.md` | GitHub 首页展示文档 |
| 优化记录 | `优化记录.md` | 八点优化说明 |
| 交付文档 | `项目交付文档.md` | 本文档 |
| 需求分析 | `需求分析计划.md` | 项目需求分析 |
| 实施方案 | `On-Call助手项目实施方案.md` | 项目实施计划 |
| 后端与前端代码 | `code/` | FastAPI 应用 |
| SOP 数据 | `code/data/` | `index.json` 与 SOP HTML |
| 验证脚本 | `code/scripts/` | 启动和验收脚本 |
| 测试用例 | `code/tests/` | pytest 测试 |
| 页面截图 | `screenshot/front/` | README 使用的前端截图 |

## 3. 技术栈

| 模块 | 技术 |
| --- | --- |
| 后端 API | FastAPI |
| 服务运行 | Uvicorn |
| HTML 解析 | BeautifulSoup4 |
| 页面渲染 | Jinja2 |
| 前端交互 | 原生 JavaScript + CSS |
| 语义搜索 | 本地 hybrid fallback |
| Agent | Local Agent / DeepSeek API |
| 依赖管理 | uv |
| 自动化验证 | pytest + verify.py |

## 4. 启动方式

进入代码目录：

```powershell
cd C:\Users\he\Desktop\aicoding\oncall\code
```

安装依赖：

```powershell
uv venv
uv sync
```

启动服务：

```powershell
.\scripts\run_dev.ps1
```

也可以直接启动：

```powershell
.\.venv\Scripts\uvicorn.exe app.main:app --host 127.0.0.1 --port 8000 --reload
```

## 5. 访问地址

| 页面 | 地址 | 用途 |
| --- | --- | --- |
| Phase 1 | `http://127.0.0.1:8000/v1` | 关键词搜索演示 |
| Phase 2 | `http://127.0.0.1:8000/v2` | 语义搜索演示 |
| Phase 3 | `http://127.0.0.1:8000/v3` | Agent 对话演示 |
| API 文档 | `http://127.0.0.1:8000/docs` | FastAPI 自动接口调试页面 |
| 健康检查 | `http://127.0.0.1:8000/health` | 服务状态检查 |

说明：`/docs` 是 FastAPI 自动生成的 Swagger 页面，可以直接输入参数调试接口，不是业务前端页面。

## 6. API 清单

| API | 方法 | 说明 |
| --- | --- | --- |
| `/health` | GET | 健康检查 |
| `/v1/documents` | POST | 新增或更新内存中的文档 |
| `/v1/search?q={query}` | GET | 关键词搜索 |
| `/v2/search?q={query}` | GET | 语义搜索 |
| `/v3/chat` | POST | Agent 对话 |

## 7. 核心验收点

### Phase 1：关键词搜索

| 查询 | 预期 |
| --- | --- |
| `OOM` | 返回 `sop-001` |
| `故障` | 返回多个文档 |
| `replication` | 返回空 |
| `CDN` | 返回 `sop-003` 和 `sop-010` |
| `&` | 正确处理特殊字符 |

### Phase 2：语义搜索

| 查询 | 预期 |
| --- | --- |
| `服务器挂了` | `sop-001` 和 `sop-004` 靠前 |
| `黑客攻击` | `sop-005` 靠前 |
| `机器学习模型出问题` | `sop-008` 靠前 |

### Phase 3：Agent 对话

| 问题 | 预期读取文件 |
| --- | --- |
| `数据库主从延迟超过30秒怎么处理？` | `index.json`、`sop-002.html` |
| `服务 OOM 了怎么办？` | `index.json`、`sop-001.html` |
| `P0 故障的响应流程是什么？` | `index.json` 和多个 SOP |
| `怀疑有人入侵了系统` | `index.json`、`sop-005.html` |
| `推荐结果质量下降了` | `index.json`、`sop-008.html` |

## 8. 验证方式

运行单元测试：

```powershell
cd C:\Users\he\Desktop\aicoding\oncall\code
.\.venv\Scripts\python.exe -m pytest tests
```

启动服务后运行接口验证：

```powershell
.\.venv\Scripts\python.exe scripts\verify.py --base-url http://127.0.0.1:8000
```

推荐使用一键验证：

```powershell
.\scripts\verify_all.ps1
```

预期结果：

```text
21 passed
All verification checks passed.
```

## 9. DeepSeek 配置说明

默认模式：

```text
AGENT_MODE=local
```

该模式不消耗 API，适合开发、测试和面试演示。

如需使用 DeepSeek：

```powershell
cd C:\Users\he\Desktop\aicoding\oncall\code
Copy-Item .env.example .env
```

在 `.env` 中配置：

```text
AGENT_MODE=deepseek
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_PROXY=http://127.0.0.1:7897
```

安全说明：

- `.env` 已被 `.gitignore` 忽略。
- 不应提交 API Key。
- DeepSeek 不能直接访问文件系统。
- 文件读取必须经过后端受控的 `readFile(fname)`。

## 10. Agent 安全约束

Agent 的工具调用约束如下：

- 只能使用 `readFile(fname)`。
- 只能读取 `code/data` 目录下的文件。
- 只能读取明确文件名。
- 不能列目录。
- 不能使用通配符。
- 不能路径穿越。
- 不能读取子目录。

这些约束由后端工具层实现，并由测试用例覆盖。

## 11. 已知限制

- Phase 2 当前使用本地语义 fallback，不是真实 embedding 或向量数据库。
- `POST /v1/documents` 写入内存，服务重启后不会持久化。
- DeepSeek 模式依赖网络、API Key 和代理配置。
- 本地 Agent 主要面向题目验收样例，不是通用推理系统。
- 前端页面面向演示，不包含登录、权限、多用户会话等生产功能。

## 12. 面试演示建议

推荐演示顺序：

1. 打开 `README.md`，介绍项目目标和三阶段能力。
2. 启动服务，访问 `/v1`，演示 `OOM`、`CDN`、`&`。
3. 访问 `/v2`，演示 `服务器挂了`、`黑客攻击`。
4. 访问 `/v3`，演示 `服务 OOM 了怎么办？`，重点说明 `readFile` 调用过程。
5. 打开 `/docs`，展示后端 API 可直接调试。
6. 运行 `scripts/verify_all.ps1`，展示自动化验收结果。

## 13. 交付状态

当前项目状态：

- 三个阶段功能已实现。
- 前端页面可演示。
- API 可通过 `/docs` 调试。
- 测试和验证脚本可运行。
- README、优化记录和交付文档已补齐。
- `.env` 和虚拟环境等敏感或本地文件已忽略。
