# On-Call 助手项目八点优化记录

> 本文档记录本项目在满足 `coding-exam/question-1/README.md`、`需求分析计划.md` 和 `On-Call助手项目实施方案.md` 基础上，为提升考核得分所做的 8 项优化。

## 1. Phase 1 关键词搜索准确性优化

**目标：** 严格满足题目对关键词搜索的验收要求。

已完成优化：

- 使用 BeautifulSoup 解析 SOP HTML。
- 提取 `id`、`title`、正文内容。
- 明确排除 `script`、`style`、`noscript` 标签内容。
- 支持英文大小写不敏感搜索。
- 支持中文关键词包含匹配。
- 支持特殊字符查询，例如 `&`。
- 返回 `snippet` 和 `score`，方便前端展示和面试说明。

核心验收点：

| 查询 | 预期结果 |
| --- | --- |
| `OOM` | 返回 `sop-001` |
| `故障` | 返回多个 SOP |
| `replication` | 返回空 |
| `CDN` | 返回 `sop-003`、`sop-010` |
| `&` | 能正确处理特殊字符 |

## 2. Phase 2 语义搜索稳定性优化

**目标：** 在不依赖真实 embedding 服务的情况下，保证语义搜索可运行、结果稳定、验收可复现。

已完成优化：

- 实现 `GET /v2/search?q={query}`。
- 使用本地 hybrid fallback 方案，不依赖外部向量服务。
- 为每个 SOP 维护语义画像，包括自然语言描述、领域关键词、同义表达。
- 综合关键词命中和语义画像命中计算相关性。
- 按相关性排序返回结果。
- README 中明确说明 fallback 的实现方式和已知限制。

核心验收点：

| 查询 | 预期结果 |
| --- | --- |
| `服务器挂了` | `sop-001`、`sop-004` 靠前 |
| `黑客攻击` | `sop-005` 靠前 |
| `机器学习模型出问题` | `sop-008` 靠前 |

## 3. Phase 3 Agent 工具约束优化

**目标：** 满足题目对 Agent 的安全约束，避免模型或后端越权访问文件系统。

已完成优化：

- 实现 `POST /v3/chat`。
- Agent 必须先通过 `readFile("index.json")` 获取可用 SOP 索引。
- Agent 只能读取 `data/` 目录下的明确文件名。
- 禁止列目录。
- 禁止通配符。
- 禁止路径穿越。
- 禁止读取子目录文件。
- 前端展示完整 `readFile` 工具调用过程。

安全约束示例：

| 输入 | 处理结果 |
| --- | --- |
| `readFile("sop-001.html")` | 允许 |
| `readFile("../README.md")` | 拒绝 |
| `readFile("*.html")` | 拒绝 |
| `readFile("nested/sop-001.html")` | 拒绝 |

## 4. Local / DeepSeek 双模式优化

**目标：** 开发测试时默认不消耗 API，上线前可切换 DeepSeek 做真实模型验证。

已完成优化：

- 新增 `AGENT_MODE=local` 和 `AGENT_MODE=deepseek`。
- 默认使用本地 Agent，保证项目离线可演示。
- DeepSeek 模式通过后端受控调用，不允许模型直接访问文件系统。
- DeepSeek 不可用时回退本地 Agent，避免面试演示中断。
- `.env.example` 提供配置模板。
- `.env` 已加入 `.gitignore`，避免 API Key 泄露。

配置示例：

```text
AGENT_MODE=local
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_PROXY=http://127.0.0.1:7897
```

## 5. 前端演示页面优化

**目标：** 让 `/v1`、`/v2`、`/v3` 三个页面清晰、稳定、适合面试展示。

已完成优化：

- `/v1` 支持关键词输入、示例查询、结果展示。
- `/v2` 支持自然语言输入、示例查询、语义结果展示。
- `/v3` 支持对话输入、回答展示、工具调用展示。
- 页面不引入复杂前端框架，降低启动和部署成本。
- 使用统一 CSS 和原生 JavaScript，结构简单、便于阅读。
- 首页展示方式与 README 截图保持一致。

演示入口：

```text
http://127.0.0.1:8000/v1
http://127.0.0.1:8000/v2
http://127.0.0.1:8000/v3
```

## 6. 验证脚本与测试覆盖优化

**目标：** 用自动化手段证明项目符合题目要求，降低人工验收不稳定性。

已完成优化：

- 增加 `pytest` 单元测试。
- 增加 `scripts/verify.py` 接口验收脚本。
- 增加 `scripts/verify_all.ps1` 一键验证脚本。
- 验证脚本输出 `PASS/FAIL`，便于面试官快速判断。
- 覆盖 Phase 1、Phase 2、Phase 3 和安全约束。

验证命令：

```powershell
cd C:\Users\he\Desktop\aicoding\oncall\code
.\scripts\verify_all.ps1
```

当前验证结果：

```text
21 passed
All verification checks passed.
```

## 7. README 与展示材料优化

**目标：** 让项目在 GitHub 首页和面试提交时更容易被理解。

已完成优化：

- 在 `oncall/README.md` 增加项目总览。
- 使用图标、badge、截图提升可读性和吸引力。
- 明确展示三阶段能力、技术栈、启动方式和验收方式。
- README 使用 `oncall/screenshot/front` 下的页面截图。
- `code/README.md` 保留详细开发说明、API 示例、评分点对照表。
- 删除重复且乱码的 `code/docs/screenshots/README.md`，避免混淆。

截图位置：

```text
oncall/screenshot/front/v1.png
oncall/screenshot/front/v2.png
oncall/screenshot/front/v3.png
```

## 8. 项目结构与提交安全优化

**目标：** 保持目录清晰、仓库干净、交付物安全。

已完成优化：

- 代码统一放在 `oncall/code`。
- SOP 数据放在 `oncall/code/data`。
- 截图放在 `oncall/screenshot`。
- 文档放在 `oncall` 根目录和 `oncall/code/README.md`。
- 添加 `.gitignore`，忽略 `.env`、虚拟环境、缓存文件、测试缓存等。
- 不提交用户本地 API Key。
- 已推送到 GitHub 仓库。

核心目录：

```text
oncall/
  README.md
  优化记录.md
  项目交付文档.md
  需求分析计划.md
  On-Call助手项目实施方案.md
  code/
    app/
    data/
    scripts/
    tests/
  screenshot/
    front/
```

## 总结

这 8 项优化的重点不是堆功能，而是围绕考核评分点提升：

- 正确性：每个阶段的验收查询都可复现。
- 稳定性：默认本地可运行，不依赖外部 API。
- 安全性：Agent 文件访问受到严格限制。
- 可维护性：后端服务、搜索逻辑、Agent、测试和文档分层清晰。
- 展示效果：前端页面、截图、README、验证脚本都能支持面试演示。
