# 页面截图

面试提交时可以在服务启动后访问以下页面截图，并替换到本目录：

- `http://127.0.0.1:8000/v1`：关键词搜索页
- `http://127.0.0.1:8000/v2`：语义搜索页
- `http://127.0.0.1:8000/v3`：Agent 对话页，重点展示 `readFile` 工具调用记录

当前项目不依赖浏览器自动化工具，因此仓库中只保留截图说明，避免引入额外依赖。
