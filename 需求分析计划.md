# On-Call 助手项目需求分析计划

## 1. 项目背景与目标

本项目来自编程面试题 `question-1`，目标是构建一个 On-Call 助手 Web 应用，帮助值班人员从部门 SOP 文档中快速找到故障处理方案，并通过对话方式回答常见线上问题。

题目提供 `data/` 目录作为知识来源，目录中包含部门 On-Call SOP 的 HTML 文档。当前 demo 数据包含 10 份 SOP，覆盖后端服务、数据库 DBA、前端、SRE、安全、数据平台、移动端、AI 算法、QA、网络 CDN 等团队。

系统需要同时提供 HTTP API 和前端页面，并按三个阶段独立实现：

| 阶段 | 名称 | 分值 | 核心目标 |
| --- | --- | --- | --- |
| Phase 1 | 搜索引擎 | 30 | 基于关键词检索 SOP 文档 |
| Phase 2 | 语义搜索 | 30 | 支持非精确匹配的语义相关搜索 |
| Phase 3 | On-Call 助手 Agent | 40 | 通过对话和受限工具读取 SOP，回答故障处理问题 |

项目最终交付目标是：用户可以在浏览器中访问不同阶段页面，通过搜索或对话快速获得相关 SOP、处理步骤和排查建议。

## 2. 用户角色与使用场景

### 2.1 主要用户

| 用户角色 | 需求 |
| --- | --- |
| 一线值班工程师 | 在收到告警后快速搜索相关 SOP，确认排查步骤、升级路径和禁止操作 |
| 技术负责人 | 在重大故障中快速了解跨团队处理流程，辅助协调升级 |
| 面试评审人员 | 通过接口和页面验证候选人的搜索、语义检索和 Agent 工程实现能力 |

### 2.2 典型使用场景

1. 值班人员看到服务 OOM 告警，输入 `OOM` 或“服务 OOM 了怎么办”，系统返回后端服务 SOP 的排查与处理建议。
2. 数据库主从延迟超过 30 秒，用户通过对话提问，Agent 定位并读取 `sop-002.html`，总结处理步骤。
3. 用户输入“黑客攻击”或“怀疑有人入侵了系统”，系统应识别为安全事件，并优先返回安全团队 SOP。
4. 用户询问“P0 故障的响应流程是什么”，Agent 应综合多个 SOP 中的升级流程给出完整回答。
5. 用户输入“机器学习模型出问题”或“推荐结果质量下降了”，系统应定位 AI 算法 SOP。

## 3. 数据来源与业务对象

### 3.1 数据来源

数据来自 `coding-exam/question-1/data/` 下的 HTML 文件：

| 文件 | 部门 | 关键内容 |
| --- | --- | --- |
| `sop-001.html` | 后端服务 | OOM 排查、服务超时、降级策略、故障分级 |
| `sop-002.html` | 数据库 DBA | 主从延迟、慢查询、连接池满、数据恢复 |
| `sop-003.html` | 前端 | 页面白屏、CDN 资源加载失败、兼容性、性能劣化 |
| `sop-004.html` | SRE | K8s 集群问题、监控告警、容量规划、故障响应 |
| `sop-005.html` | 安全团队 | 安全事件分级、入侵检测、漏洞响应 |
| `sop-006.html` | 数据平台 | 数据管道故障、ETL 失败、Spark 集群 |
| `sop-007.html` | 移动端 | App 崩溃率、热修复、推送服务 |
| `sop-008.html` | AI & 算法 | 模型推理延迟、推荐质量下降、GPU 集群 |
| `sop-009.html` | QA | 测试环境故障、自动化测试、发版卡点 |
| `sop-010.html` | 网络 & CDN | CDN 节点故障、DNS 异常、DDoS 防护 |

### 3.2 核心业务对象

| 对象 | 说明 |
| --- | --- |
| SOP 文档 | 一份部门 On-Call 标准处理流程，来源为 HTML |
| 文档标题 | 从 HTML 的 `<title>` 或 `<h1>` 中提取，用于搜索结果展示 |
| 正文内容 | 从 HTML body 中提取，应排除 `script`、`style` 等非正文标签 |
| 检索结果 | 包含文档 ID、标题、摘要片段和相关性分数 |
| Agent 工具调用 | Agent 调用 `readFile(fname)` 读取文件的过程，必须展示给用户 |

## 4. Phase 1：关键词搜索需求分析

### 4.1 目标

实现一个基础搜索引擎，使用户可以通过关键词搜索 SOP 文档。

### 4.2 API 需求

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/v1/documents` | POST | 新增或更新文档，入参包含 `id` 和 `html` |
| `/v1/search?q={query}` | GET | 根据关键词搜索文档 |
| `/v1` | GET | 关键词搜索页面 |

`POST /v1/documents` 期望请求与响应：

```json
{
  "id": "sop-001",
  "html": "<html>...</html>"
}
```

```json
{
  "id": "sop-001",
  "title": "后端服务 On-Call SOP"
}
```

`GET /v1/search` 期望响应：

```json
{
  "query": "OOM",
  "results": [
    {
      "id": "sop-001",
      "title": "后端服务 On-Call SOP",
      "snippet": "...单服务OOM崩溃...",
      "score": 1.0
    }
  ]
}
```

### 4.3 功能规则

1. 搜索范围应是清洗后的正文和标题，不应包含 `script`、`style`、`noscript` 等标签内容。
2. 英文关键词应支持大小写不敏感匹配，例如 `OOM`、`oom` 都能命中。
3. 中文关键词应支持直接包含匹配，例如 `故障` 应返回多个 SOP。
4. 搜索结果应按相关性分数排序。
5. 搜索结果应返回 snippet，帮助用户判断命中位置。
6. 特殊字符查询需要正确处理，例如 `&`。

### 4.4 验收标准

| 查询 | 期望结果 |
| --- | --- |
| `GET /v1/search?q=OOM` | 返回 `sop-001` |
| `GET /v1/search?q=故障` | 返回多个文档 |
| `GET /v1/search?q=replication` | 返回空，因为该词只应出现在 `script` 标签中 |
| `GET /v1/search?q=CDN` | 返回 `sop-003` 和 `sop-010` |
| `GET /v1/search?q=&` | 返回正文中包含 `&` 字符的文档 |

## 5. Phase 2：语义搜索需求分析

### 5.1 目标

在关键词搜索基础上，实现语义搜索。用户输入的查询词不需要在文档中精确出现，只要语义相关，就应该返回对应 SOP。

### 5.2 API 需求

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/v2/search?q={query}` | GET | 根据语义相关性搜索文档 |
| `/v2` | GET | 语义搜索页面 |

`GET /v2/search` 期望响应：

```json
{
  "query": "服务器挂了",
  "results": [
    {
      "id": "sop-001",
      "title": "后端服务 On-Call SOP",
      "snippet": "...服务超时、OOM、降级...",
      "score": 0.87
    }
  ]
}
```

### 5.3 功能规则

1. 支持自然语言查询，不要求查询词在 SOP 中完全出现。
2. 结果必须按相关性排序。
3. 应优先复用 Phase 1 的 HTML 清洗、文档结构化和结果展示能力。
4. 可采用 embedding、关键词增强、同义词映射或 hybrid search 实现，但最终行为必须满足题目验证样例。
5. 语义搜索结果仍需包含 `id`、`title`、`snippet` 和 `score`。

### 5.4 验收标准

| 查询 | 期望结果 |
| --- | --- |
| `GET /v2/search?q=服务器挂了` | `sop-001` 和 `sop-004` 靠前 |
| `GET /v2/search?q=黑客攻击` | `sop-005` 靠前 |
| `GET /v2/search?q=机器学习模型出问题` | `sop-008` 靠前 |

## 6. Phase 3：On-Call 助手 Agent 需求分析

### 6.1 目标

实现一个能通过对话回答 On-Call 问题的 Agent。Agent 需要根据用户问题定位相关 SOP，通过受限工具读取文件，然后总结可执行的排查和处理建议。

### 6.2 API 需求

题目只固定要求：

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/v3` | GET | 对话页面，包含消息输入和对话历史 |

对话 API 可自行定义。建议实现一个 POST 接口，例如：

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/v3/chat` | POST | 接收用户消息，返回 Agent 答案和工具调用记录 |

建议请求格式：

```json
{
  "message": "数据库主从延迟超过30秒怎么处理？"
}
```

建议响应格式：

```json
{
  "answer": "建议先确认主从复制状态、检查延迟来源...",
  "toolCalls": [
    {
      "tool": "readFile",
      "args": {
        "fname": "sop-002.html"
      }
    }
  ]
}
```

### 6.3 工具限制

Agent 只能使用一个工具：

```text
readFile(fname: string) -> string
```

限制必须严格遵守：

1. 只能读取 `data/` 目录下文件。
2. 只能按明确文件名读取。
3. 不能列目录。
4. 不能使用通配符。
5. 对话过程必须展示工具调用过程。
6. 可以向 `data/` 目录添加辅助索引文件，但 Agent 仍必须通过 `readFile("具体文件名")` 读取。

### 6.4 功能规则

1. Agent 应先理解用户问题所属领域，例如后端、数据库、安全、AI 算法等。
2. Agent 应定位一个或多个相关 SOP 文件。
3. Agent 应读取相关文件后再回答，不能只凭空生成。
4. 回答应包含具体处理步骤、排查方向、升级建议和必要的禁止操作。
5. 当问题涉及 P0 或跨团队故障时，允许综合多个 SOP 给出答案。
6. 前端需要展示用户消息、Agent 回复和工具调用记录。

### 6.5 验收标准

| 用户提问 | 期望行为 |
| --- | --- |
| 数据库主从延迟超过30秒怎么处理？ | Agent 定位并读取 `sop-002.html`，给出处理步骤 |
| 服务 OOM 了怎么办？ | Agent 找到 `sop-001.html`，给出排查和处理建议 |
| P0 故障的响应流程是什么？ | Agent 综合多个 SOP 给出完整回答 |
| 怀疑有人入侵了系统 | Agent 找到 `sop-005.html`，给出安全事件响应流程 |
| 推荐结果质量下降了 | Agent 找到 `sop-008.html`，给出排查方向 |

## 7. 前端页面需求

题目对前端样式不做要求，但必须能演示功能。

| 页面 | 路由 | 最低要求 |
| --- | --- | --- |
| 关键词搜索页 | `/v1` | 输入框、搜索按钮、结果列表 |
| 语义搜索页 | `/v2` | 输入框、搜索按钮、结果列表 |
| Agent 对话页 | `/v3` | 消息输入、发送按钮、对话历史、工具调用过程展示 |

前端结果展示建议包含：

1. 文档 ID。
2. 文档标题。
3. 命中片段或 Agent 回答。
4. 分数或相关性说明。
5. Agent 阶段的工具调用文件名。

## 8. 非功能需求

### 8.1 正确性

系统必须满足 README 中的验证样例。特别注意 HTML 清洗逻辑，不能把 `script` 中的内容作为正文参与搜索。

### 8.2 可演示性

面试评审人员应能通过浏览器和 HTTP 请求验证所有阶段功能。启动方式、接口示例和验证命令应在最终项目 README 中说明。

### 8.3 可维护性

文档解析、搜索逻辑、语义检索和 Agent 逻辑应尽量解耦，避免三个阶段重复实现同一套 HTML 清洗代码。

### 8.4 可扩展性

虽然 demo 只有 10 份 SOP，但题目背景是 100 份文档。实现时应避免写死只支持 10 个文件，允许后续新增 SOP。

### 8.5 安全性

Agent 的 `readFile` 工具必须限制在 `data/` 目录内，避免通过 `../` 等路径读取项目外文件。

## 9. 实施优先级与里程碑

### 9.1 里程碑一：基础文档处理

目标：完成 SOP HTML 解析和文档结构化。

输出：

1. 可读取 `data/` 下所有 HTML。
2. 可提取文档 ID、标题、正文。
3. 可排除 `script`、`style` 等非正文内容。

### 9.2 里程碑二：Phase 1 关键词搜索

目标：完成 `/v1/documents`、`/v1/search` 和 `/v1` 页面。

验收重点：

1. `OOM` 命中 `sop-001`。
2. `replication` 返回空。
3. `CDN` 命中 `sop-003` 和 `sop-010`。

### 9.3 里程碑三：Phase 2 语义搜索

目标：完成 `/v2/search` 和 `/v2` 页面。

验收重点：

1. “服务器挂了”能召回后端和 SRE。
2. “黑客攻击”能召回安全 SOP。
3. “机器学习模型出问题”能召回 AI 算法 SOP。

### 9.4 里程碑四：Phase 3 Agent

目标：完成 `/v3` 对话页和对话 API。

验收重点：

1. Agent 能通过 `readFile(fname)` 读取 SOP。
2. Agent 不能列目录或通配读取。
3. 前端展示工具调用过程。
4. 五个 README 对话样例均能得到符合预期的答案。

### 9.5 里程碑五：项目整理与提交

目标：补充最终 README、运行说明和验证方法。

输出：

1. 启动命令。
2. API 示例。
3. 测试或手动验证命令。
4. 已知限制。
5. 如使用外部大模型或 embedding 服务，说明所需 API Key。

## 10. 风险、难点与建议方案

| 风险或难点 | 影响 | 建议方案 |
| --- | --- | --- |
| HTML 中包含 `script` 内容 | 关键词搜索误命中，例如 `replication` | 解析正文前删除 `script`、`style`、`noscript` 标签 |
| 查询中包含特殊字符 `&` | URL 解析可能变成空查询 | 前端 encode 查询参数，后端正确读取原始 query |
| 语义搜索依赖外部模型 | API Key 或网络不可用会影响演示 | 提供 fallback，例如关键词增强、同义词映射或本地轻量方案 |
| Agent 不能列目录 | 无法动态发现文件 | 提前准备索引文件，Agent 通过 `readFile("index.json")` 读取 |
| Agent 凭空回答 | 答案不可信，可能偏离 SOP | 要求先读取相关 SOP，再根据内容总结 |
| P0 问题可能跨多个 SOP | 单文件答案不完整 | 允许读取多个相关 SOP，综合升级流程 |

## 11. 总体验收清单

### 11.1 API 验收

- [ ] `POST /v1/documents` 可新增或更新文档。
- [ ] `GET /v1/search?q=OOM` 返回 `sop-001`。
- [ ] `GET /v1/search?q=故障` 返回多个文档。
- [ ] `GET /v1/search?q=replication` 返回空。
- [ ] `GET /v1/search?q=CDN` 返回 `sop-003` 和 `sop-010`。
- [ ] `GET /v1/search?q=&` 能正确处理特殊字符。
- [ ] `GET /v2/search?q=服务器挂了` 返回 `sop-001` 和 `sop-004` 靠前。
- [ ] `GET /v2/search?q=黑客攻击` 返回 `sop-005` 靠前。
- [ ] `GET /v2/search?q=机器学习模型出问题` 返回 `sop-008` 靠前。
- [ ] `/v3` 页面可完成对话。
- [ ] Agent 响应中展示 `readFile` 工具调用过程。

### 11.2 页面验收

- [ ] `/v1` 可以输入关键词并展示搜索结果。
- [ ] `/v2` 可以输入自然语言并展示语义搜索结果。
- [ ] `/v3` 可以输入问题并展示对话历史。
- [ ] `/v3` 可以展示 Agent 读取了哪些文件。

### 11.3 Agent 验收

- [ ] “数据库主从延迟超过30秒怎么处理？”读取 `sop-002.html`。
- [ ] “服务 OOM 了怎么办？”读取 `sop-001.html`。
- [ ] “P0 故障的响应流程是什么？”综合多个 SOP。
- [ ] “怀疑有人入侵了系统”读取 `sop-005.html`。
- [ ] “推荐结果质量下降了”读取 `sop-008.html`。

## 12. 推荐实现顺序

对于新手，建议不要一开始就写 Agent。推荐顺序如下：

1. 先完成 HTML 读取、清洗和结构化。
2. 再完成 Phase 1 关键词搜索。
3. 复用文档数据结构，实现 Phase 2 语义搜索。
4. 准备 Agent 可读取的索引文件。
5. 实现 Phase 3 对话和 `readFile` 工具。
6. 最后补齐前端页面、README 和验证脚本。

这样可以保证每一步都有可运行结果，也更符合题目“按顺序完成，每阶段完整实现才得分”的要求。
